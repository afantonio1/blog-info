<div class="mensagemAlerta">
    {{#each arrayErros}}
        <div class="alert alert-danger">
            {{texto}}
            <button type="button" class="close" data-dismiss="alert" aria-label="Fechar">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    {{else}}

    {{/each}}
</div>

<h4>Editar Categoria</h4>
<div class="card">
    <div class="card-body">
        <form id="frmCatEdit" action="/admin/categorias/edit" method="post">
            <input type="hidden" name="id" id="id" value="{{categoria._id}}">
            <div class="form-group">
                <label for="nome"> Nome: </label>
                <input class="form-control" type="text" id="nome" name="nome" placeholder="Nome da categoria" value="{{categoria.nome}}">
            </div>
            <div class="form-group">
                <label for="slug"> Slug: </label>
                <input class="form-control" type="text" id="slug" name="slug" placeholder="Slug da categoria" value="{{categoria.slug}}">
            </div>
        </form>
         <div class="boxBotoes">
            <button class="btn btn-info" onclick="document.getElementById('frmCatEdit').submit();" title="Atualizar categoria {{categoria.nome}}">Atualizar</button>
            <button class="btn btn-danger" onclick="document.location.href='/admin/categorias'" title="Cancelar edição de categoria {{categoria.nome}}">Cancelar</button></a>
        </div>
    </div>
</div>